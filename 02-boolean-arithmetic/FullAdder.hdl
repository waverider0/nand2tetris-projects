CHIP FullAdder {
  IN a, b, c;
  OUT sum, carry;

  PARTS:
    HalfAdder(a=b, b=c, sum=SUMab, carry=CARab);
    HalfAdder(a=a, b=SUMab, sum=sum, carry=CARaSUMab);
    Or(a=CARaSUMab, b=CARab, out=carry);
}
